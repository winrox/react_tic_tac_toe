var React = require('react');

module.exports = React.createClass({
  render: function() {
    return <td>
      <img id={} url={} onClick={...handleTileClick}/>
    </td>
  },

  handleTileClick: function() {
    return {} //hook this up later
  }
});
